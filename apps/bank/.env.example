# ─── Feature Flags ────────────────────────────────────────────────────────────
# When running as a standalone app (yarn dev:standalone:bank).
# In the single-domain setup (apps/web), this flag is controlled via apps/web/.env.

PUBLIC_FEATURE_BANK=true
