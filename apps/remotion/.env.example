# ─── Feature Flags ────────────────────────────────────────────────────────────
# When running as a standalone app (yarn dev:standalone:remotion).
# In the single-domain setup (apps/web), this flag is controlled via apps/web/.env.

PUBLIC_FEATURE_REMOTION=true
